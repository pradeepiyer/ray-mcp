name: Codex Tests

on: [push, pull_request]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Format
      run: make format
    - name: Test Fast Suite
      run: make test-fast
