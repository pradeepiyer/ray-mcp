# Core MCP and Ray dependencies
mcp>=1.0.0
ray[default]>=2.8.0
typing-extensions>=4.5.0
pydantic>=2.0.0
requests>=2.28.0
psutil>=5.9.0

# Data processing and analysis
pandas>=1.5.0
numpy>=1.21.0
pyarrow>=10.0.0

# Machine Learning dependencies (Ray Train/Tune/Serve)
# Install specific ML frameworks as needed:
# ray[train]>=2.8.0       # For distributed training
# ray[tune]>=2.8.0        # For hyperparameter tuning  
# ray[serve]>=2.8.0       # For model serving
# ray[data]>=2.8.0        # For distributed data processing
# ray[workflows]>=2.8.0   # For workflow orchestration

# Optional ML framework support
torch>=1.13.0;python_version>="3.8"
tensorflow>=2.12.0;python_version>="3.8"
scikit-learn>=1.1.0
xgboost>=1.6.0

# Workflow and scheduling
croniter>=1.3.0
schedule>=1.2.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0

# Additional utilities
click>=8.0.0
rich>=13.0.0
tqdm>=4.64.0 